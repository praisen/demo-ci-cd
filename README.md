# Demo CI/CD (Jenkins + Maven + SonarQube + Dependency-Check + Docker + Trivy)

Minimal Spring Boot app to use with the Jenkins Pipeline in the chat.

## Run locally
```bash
mvn clean package
java -jar target/demo-0.0.1-SNAPSHOT.jar
# open http://localhost:8080
```
